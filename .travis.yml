language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t yooncheol14/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true yooncheol14/react-test-app npm run test

after_success:
  - docker build -t yooncheol14/docker-frontend ./frontend
  - docker build -t yooncheol14/docker-backend ./backend
  - docker build -t yooncheol14/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push yooncheol14/docker-frontend
  - docker push yooncheol14/docker-backend
  - docker push yooncheol14/docker-nginx
